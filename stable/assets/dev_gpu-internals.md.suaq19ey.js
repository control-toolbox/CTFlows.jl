import{_ as t,o as a,c as s,ao as o}from"./chunks/framework.DDgD3ozX.js";const u=JSON.parse('{"title":"GPU internals","description":"","frontmatter":{},"headers":[],"relativePath":"dev/gpu-internals.md","filePath":"dev/gpu-internals.md","lastUpdated":null}'),i={name:"dev/gpu-internals.md"};function n(r,e,l,d,c,h){return a(),s("div",null,[...e[0]||(e[0]=[o('<h1 id="GPU-internals" tabindex="-1">GPU internals <a class="header-anchor" href="#GPU-internals" aria-label="Permalink to &quot;GPU internals {#GPU-internals}&quot;">​</a></h1><p>How GPU support is built, why it is built that way, and what to know before changing it. The user-facing side lives in <a href="./../flows/gpu">GPU flows</a>; this page is the design record.</p><hr><h2 id="Architecture:-the-device-parameter-lives-in-the-strategies" tabindex="-1">Architecture: the device parameter lives in the strategies <a class="header-anchor" href="#Architecture:-the-device-parameter-lives-in-the-strategies" aria-label="Permalink to &quot;Architecture: the device parameter lives in the strategies {#Architecture:-the-device-parameter-lives-in-the-strategies}&quot;">​</a></h2><p>GPU support is spread across three packages, and the load-bearing idea is a single one:</p><blockquote><p><strong>The device is a parameter of the <em>strategies</em>, not a property of the flow.</strong></p></blockquote><p>A flow never knows it is &quot;a GPU flow&quot;. It was handed strategies that happen to be parameterised on <code>GPU</code>, and it is called with device arrays.</p><table tabindex="0"><thead><tr><th style="text-align:right;">Package</th><th style="text-align:right;">What it contributes</th></tr></thead><tbody><tr><td style="text-align:right;">CTBase</td><td style="text-align:right;">the <code>CPU</code>/<code>GPU</code> strategy parameters, and <code>Differentiation.DifferentiationInterface{P}</code> with a <strong>per-device default AD backend</strong></td></tr><tr><td style="text-align:right;">CTSolvers</td><td style="text-align:right;"><code>Integrators.SciML{P}</code> and the per-device metadata seam</td></tr><tr><td style="text-align:right;">CTFlows</td><td style="text-align:right;">a registry declaring both families as <code>[CPU, GPU]</code>, plus the routing that turns <code>method=:gpu</code> into resolved strategies</td></tr></tbody></table><p>The alternative — a distinct <code>GPUStateFlow</code> type, or a device field on the flow — was rejected: it would have duplicated the whole flow type hierarchy for something that only changes <em>how two strategies are built</em>. Parameterising the strategies kept the flow layer untouched.</p><p>Both parameterisations are backward-compatible by construction: <code>SciML()</code> builds <code>SciML{CPU}</code> and <code>DifferentiationInterface()</code> builds <code>DifferentiationInterface{CPU}</code>, so every pre-existing call site behaves exactly as before.</p><div class="warning custom-block"><p class="custom-block-title">Parameterising a strategy a consumer registers bare</p><p>When <code>SciML</code> was parameterised, the bare method <code>parameter(::Type{&lt;:SciML}) = nothing</code> was dropped in favour of the parameterised one. <code>AbstractIntegrator</code> has <strong>no family-level fallback</strong> (unlike CTBase&#39;s <code>AbstractADBackend</code>), and CTFlows registers <code>SciML</code> <em>bare</em> — so <code>parameter(SciML)</code> started throwing and broke every existing flow path. The fix is to keep the bare method alongside the parameterised one; they are unambiguous by specificity.</p><p>The regression was invisible to CTSolvers&#39; own suite, which never calls <code>parameter(SciML)</code> bare. It was caught only by a backward-compatibility check run from the <em>consumer</em>. Worth repeating whenever a strategy gains a parameter.</p></div><hr><h2 id="The-path-of-method:gpu-through-the-code" tabindex="-1">The path of <code>method=:gpu</code> through the code <a class="header-anchor" href="#The-path-of-method:gpu-through-the-code" aria-label="Permalink to &quot;The path of `method=:gpu` through the code {#The-path-of-method:gpu-through-the-code}&quot;">​</a></h2><p><code>method</code> is a plain keyword that rides along in every constructor&#39;s <code>kwargs...</code> and is removed at one place — the routing boundary. That choice is deliberate: threading a device argument through the constructor signatures would have touched roughly twenty of them. 2. <a href="/CTFlows.jl/stable/api/internals#CTFlows.Flows._pop_method"><code>CTFlows.Flows._pop_method</code></a> splits <code>(method, rest)</code> out of the keyword arguments, so <code>method</code> is never mistaken for a strategy option (<a href="https://github.com/control-toolbox/CTFlows.jl/blob/main/src/Flows/flow_routing.jl" target="_blank" rel="noreferrer"><code>src/Flows/flow_routing.jl</code></a>).</p><ol start="3"><li><a href="/CTFlows.jl/stable/api/internals#CTFlows.Flows._flow_description"><code>CTFlows.Flows._flow_description</code></a> completes the user tokens against <a href="/CTFlows.jl/stable/api/internals#CTFlows.Flows._available_flow_methods"><code>CTFlows.Flows._available_flow_methods</code></a>, dispatched on <code>Traits.ad_trait</code>:</li></ol><ul><li><p><code>WithAD</code> → <code>(:di, :sciml, :cpu)</code> / <code>(:di, :sciml, :gpu)</code></p></li><li><p><code>WithoutAD</code> → <code>(:sciml, :cpu)</code> / <code>(:sciml, :gpu)</code></p></li></ul><p>One <code>:gpu</code> token therefore resolves the device for <strong>both</strong> families at once.</p><ol start="4"><li><code>Orchestration.resolve_method</code> matches the description against <a href="/CTFlows.jl/stable/api/flows#CTFlows.Flows.flow_registry"><code>CTFlows.Flows.flow_registry</code></a> and builds the concrete strategies.</li></ol><div class="tip custom-block"><p class="custom-block-title">Base descriptions must carry <code>:cpu</code> explicitly</p><p>Because the registry is parameterised, <code>extract_global_parameter_from_method</code> requires a device token and throws without one. The base (default) descriptions therefore spell out <code>:cpu</code> rather than omitting it. <code>(:di, :sciml)</code> alone does not work.</p></div><p>This routing also unified two previously separate construction paths (with and without AD) behind a single <code>_build_integrator</code> helper: every flow constructor now reaches the integrator through the registry, so a device token is honoured no matter which constructor was called.</p><h3 id="what-sciml-gpu-does-today-what-sciml-gpu-does-today" tabindex="-1">What <code>SciML{GPU}</code> does today {#What-SciML{GPU}-does-today} <a class="header-anchor" href="#what-sciml-gpu-does-today-what-sciml-gpu-does-today" aria-label="Permalink to &quot;What `SciML{GPU}` does today {#What-SciML{GPU}-does-today}&quot;">​</a></h3><p>Honestly: not much on its own. The runtime device behaviour in CTFlows is driven by <strong>array type dispatch</strong> (<code>GPUArraysCore.AbstractGPUArray</code>), not by the integrator parameter. The practical effect of <code>method=:gpu</code> today is selecting the GPU AD default.</p><p><code>SciML{P}</code> carries a metadata seam and CTSolvers defines <code>Integrators.__consistent_initial_condition(P, u0)</code> — a validator that flags a <code>CuArray</code> <code>u0</code> under <code>SciML{CPU}</code> or a host <code>Array</code> under <code>SciML{GPU}</code>. <strong>CTFlows does not call it yet.</strong> It was deliberately left in CTSolvers, next to the integrator, because <code>SciML</code> does not receive <code>u0</code> at build time; wiring it in at problem construction is an open, self-contained improvement that would turn a class of confusing device errors into a clear message.</p><hr><h2 id="The-coercion-helpers" tabindex="-1">The coercion helpers <a class="header-anchor" href="#The-coercion-helpers" aria-label="Permalink to &quot;The coercion helpers {#The-coercion-helpers}&quot;">​</a></h2><p>Three small functions in <a href="https://github.com/control-toolbox/CTFlows.jl/blob/main/src/Systems/coercion.jl" target="_blank" rel="noreferrer"><code>src/Systems/coercion.jl</code></a> carry essentially all the device-awareness of the package. Each solves a distinct problem, and each is dispatched on <code>GPUArraysCore.AbstractGPUArray</code> so it stays backend-agnostic.</p><h3 id="CTFlows.Systems._safe_only@ref-—-the-1-D-scalar-convention" tabindex="-1"><a href="/CTFlows.jl/stable/api/internals#CTFlows.Systems._safe_only"><code>CTFlows.Systems._safe_only</code></a> — the 1-D = scalar convention <a class="header-anchor" href="#CTFlows.Systems._safe_only@ref-—-the-1-D-scalar-convention" aria-label="Permalink to &quot;[`CTFlows.Systems._safe_only`](/api/internals#CTFlows.Systems._safe_only) — the 1-D = scalar convention {#CTFlows.Systems._safe_only@ref-—-the-1-D-scalar-convention}&quot;">​</a></h3><p>CTFlows represents a one-dimensional state as a scalar, and the collapse is <code>only(x)</code>. On a device array <code>only</code> resolves through <code>iterate → getindex</code>: a scalar read, rejected under <code>allowscalar(false)</code>. <code>_safe_only</code> wraps it in <code>@allowscalar</code> for <code>AbstractGPUArray</code> and is plain <code>only</code> otherwise.</p><p>It is a genuine host synchronisation, but it only ever fires for the degenerate length-1 case — real GPU workloads have <code>length ≥ 2</code>. The alternative (rejecting 1-D on device) was considered and dropped: correct-but-slow beats an error at a boundary users cross by accident.</p><h3 id="CTFlows.Systems._device_like@ref-—-augmented-Hamiltonian-blocks" tabindex="-1"><a href="/CTFlows.jl/stable/api/internals#CTFlows.Systems._device_like"><code>CTFlows.Systems._device_like</code></a> — augmented Hamiltonian blocks <a class="header-anchor" href="#CTFlows.Systems._device_like@ref-—-augmented-Hamiltonian-blocks" aria-label="Permalink to &quot;[`CTFlows.Systems._device_like`](/api/internals#CTFlows.Systems._device_like) — augmented Hamiltonian blocks {#CTFlows.Systems._device_like@ref-—-augmented-Hamiltonian-blocks}&quot;">​</a></h3><p>Augmented flows (<code>variable_costate=true</code>) assemble <code>[∂p; -∂x; -∂pv]</code>. The <code>∂pv</code> block is computed host-side, because the variable <code>v</code> stays on the host by design. Assigning a host array into a slice of a device <code>du</code> puts a host array inside a <code>Broadcasted</code>, which fails to compile with a <code>GPUCompiler.KernelError</code>.</p><p><code>_device_like(dst, src)</code> adapts the host block to <code>dst</code>&#39;s array type. The alternative — carrying <code>v</code> on the device — was rejected: it would have pushed device-residency into the variable handling everywhere, whereas one adaptation at the assignment point fixes all four augmented functors at once.</p><h3 id="CTFlows.Systems._buffer_like@ref-—-the-in-place-dynamics-buffer" tabindex="-1"><a href="/CTFlows.jl/stable/api/internals#CTFlows.Systems._buffer_like"><code>CTFlows.Systems._buffer_like</code></a> — the in-place dynamics buffer <a class="header-anchor" href="#CTFlows.Systems._buffer_like@ref-—-the-in-place-dynamics-buffer" aria-label="Permalink to &quot;[`CTFlows.Systems._buffer_like`](/api/internals#CTFlows.Systems._buffer_like) — the in-place dynamics buffer {#CTFlows.Systems._buffer_like@ref-—-the-in-place-dynamics-buffer}&quot;">​</a></h3><p>The OCP right-hand sides allocated their <code>dynamics!</code> buffer as <code>Vector{T}(undef, n)</code>, pinning it to the host regardless of the state. Eleven sites. The obvious fix, <code>similar(xs, T, n)</code>, is <strong>not</strong> a safe drop-in: under the 1-D = scalar convention <code>xs</code> may be a <code>Number</code>, which has no useful <code>similar</code>. Hence:</p><div class="language-julia vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">julia</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">_buffer_like</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(template</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">::</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">AbstractArray</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">::</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">Type{T}</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, n</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">::</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">Int</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">) </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">where</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> {T} </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> similar</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(template, T, n)</span></span>\n<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">_buffer_like</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">::</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">Any</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">::</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">Type{T}</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, n</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">::</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">Int</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">) </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">where</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> {T} </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> Vector{T}</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(undef, n)</span></span></code></pre></div><div class="tip custom-block"><p class="custom-block-title">Measured cost on the CPU path: zero</p><p>A Goddard benchmark exercising the OCP pseudo-Hamiltonian under ForwardDiff reported <strong>identical</strong> memory and allocation counts before and after the migration, with values identical to the last digit. The reason is that the OCP right-hand side receives the state as a <code>SubArray</code>, and <code>similar(::SubArray, T, n)</code> allocates the same plain <code>Vector{T}</code>.</p></div><hr><h2 id="Decisions,-and-what-was-rejected" tabindex="-1">Decisions, and what was rejected <a class="header-anchor" href="#Decisions,-and-what-was-rejected" aria-label="Permalink to &quot;Decisions, and what was rejected {#Decisions,-and-what-was-rejected}&quot;">​</a></h2><p><strong>Why the variable <code>v</code> stays on the host.</strong> It parameterises the right-hand side and transits through <code>ODEParameters</code>. Julia broadcasts a host scalar against device arrays without help, and a small host vector used in device code is a compatibility question the user owns. Moving it to the device would buy nothing and complicate every path.</p><p><strong>Why materialisation gathers to the host.</strong> Point calls return device arrays and the hot path stays on the device; building a <code>CTModels.Solution</code>, plotting, and interpolation gather. Not a compromise — everything downstream of those boundaries is host code by nature.</p><p><strong>Why <code>AutoMooncake</code> is the GPU AD default.</strong> The gate is not GPU-ness, it is <em>mutation</em>: an OCP Hamiltonian fills its buffer through CTModels&#39; in-place <code>dynamics!</code>.</p><ul><li><p><code>AutoForwardDiff</code> (the CPU default) scalar-indexes device arrays — unusable on device.</p></li><li><p><code>AutoZygote</code> (the original GPU default) cannot differentiate mutation, and the failure <strong>reproduces on CPU</strong>, which proves no buffer rewrite could have lifted it. A probe campaign additionally showed its device behaviour is <strong>call-context dependent</strong>: the same Hamiltonian formula raised a <code>KernelException</code> on a direct non-mutating gradient call while succeeding as the right-hand side of a full integration. That instability, independent of mutation, is why it was demoted rather than merely supplemented.</p></li><li><p><code>AutoEnzyme</code> fails reverse-mode activity analysis through <code>GPUArrays._mapreduce</code>/<code>sum</code> on a <code>CuArray</code>, and in the real OCP call the gradient closure captures a <code>CuArray</code> without the annotation Enzyme needs. The error names its own candidate fix, <code>AutoEnzyme(; function_annotation=Enzyme.Duplicated)</code> — <strong>untried</strong>. Enzyme is not ruled out, it simply was not needed once Mooncake cleared the whole matrix.</p></li><li><p><code>AutoMooncake</code> differentiates the mutating right-hand side on device, end to end, with correct values. Mooncake also ships CUDA rules (a <code>MooncakeCUDAExt</code> that is not advertised on the DifferentiationInterface backend page).</p></li></ul><p>Rejected alternatives for the same blocker: rewriting the OCP Hamiltonians with <code>Zygote.Buffer</code> (ties the code to one backend), and adding an out-of-place dynamics contract to CTModels (cleanest in principle, but CTModels has no out-of-place API and it would have been a cross-package change).</p><div class="tip custom-block"><p class="custom-block-title">The blocker was lifted by changing a default, not by writing code</p><p>Not one line of CTFlows <code>src/</code> is attributable to the OCP-Hamiltonian-on-device blocker. It was cleared by swapping the GPU AD default upstream in CTBase. The reason this was even visible as an option is that the capability was <strong>probed on hardware before the implementation was designed</strong> — the measurement changed the plan.</p></div><div class="warning custom-block"><p class="custom-block-title">Changing an upstream default can break a downstream test environment</p><p>Swapping the GPU default to <code>AutoMooncake</code> left CTFlows&#39; test environment resolving a backend it had never listed as a dependency. The next GPU CI run would have failed with no change on the CTFlows side at all — a latent breakage introduced entirely from upstream. When you change a computed default in a strategy, check the test environments of its consumers.</p></div><div class="warning custom-block"><p class="custom-block-title">Residual: <code>AutoMooncake</code> on CPU</p><p>Verified on CPU: with a <code>@view</code>, Mooncake returns a structural <code>Mooncake.Tangent{@NamedTuple{parent, indices, offset1, stride1}}</code> — right values, inside <code>.parent</code>, but not an array — so negating it throws a <code>MethodError</code> in the Hamiltonian RHS functors. The views come from the Hamiltonian splitter. On device <code>MooncakeCUDAExt</code> yields a negatable tangent, so <strong>no CTBase extension fix is required</strong>; but a user explicitly asking for <code>AutoMooncake</code> on CPU gets a cryptic error. Not a supported combination.</p></div><hr><h2 id="Testing-on-a-device" tabindex="-1">Testing on a device <a class="header-anchor" href="#Testing-on-a-device" aria-label="Permalink to &quot;Testing on a device {#Testing-on-a-device}&quot;">​</a></h2><h3 id="The-gating-pattern" tabindex="-1">The gating pattern <a class="header-anchor" href="#The-gating-pattern" aria-label="Permalink to &quot;The gating pattern {#The-gating-pattern}&quot;">​</a></h3><p>Device tests self-gate, so a machine without a GPU skips them cleanly:</p><div class="language-julia vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">julia</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">is_cuda_on</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">() </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> CUDA</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">.</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">functional</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">()</span></span>\n<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">...</span></span>\n<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">if</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> !</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">is_cuda_on</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">()</span></span>\n<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">    @info</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &quot;CUDA not functional — GPU tests skipped&quot;</span></span>\n<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">    return</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> nothing</span></span>\n<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">end</span></span>\n<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">CUDA</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">.</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">allowscalar</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">false</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">)   </span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># every ✓ below is genuinely scalar-index-free</span></span></code></pre></div><p><code>allowscalar(false)</code> is not decoration: without it a test can pass while silently synchronising element by element, which is exactly the defect the tests exist to catch.</p><p>Two suites, deliberately separate — <a href="https://github.com/control-toolbox/CTFlows.jl/blob/main/test/suite/extensions/test_gpu_flows.jl" target="_blank" rel="noreferrer"><code>test/suite/extensions/test_gpu_flows.jl</code></a> (single flows, array-level) and <code>test_gpu_ensemble.jl</code> (the DiffEqGPU tooling proof of concept, not wired to <code>Flow</code>). Every device example in the user guide is traceable to a row in one of them.</p><h3 id="There-is-no-local-validation" tabindex="-1">There is no local validation <a class="header-anchor" href="#There-is-no-local-validation" aria-label="Permalink to &quot;There is no local validation {#There-is-no-local-validation}&quot;">​</a></h3><p><strong>A developer without a device cannot validate GPU work locally.</strong> The only real pass/fail is the <code>test-gpu-kkt</code> job on the self-hosted NVIDIA runner, triggered by the <code>run ci gpu</code> label on a pull request. Practical consequences:</p><ul><li><p>The label costs real GPU time. Do not set it on documentation-only or CPU-only changes.</p></li><li><p>The workflows listen on <code>[labeled, synchronize, reopened]</code> — <strong>not <code>opened</code></strong> — because creating a PR with a label already attached fires both events and bills two runs. <code>reopened</code> is kept: reopening does not re-emit <code>labeled</code>.</p></li><li><p>Prefer letting a run finish over cancelling it. Cancelling a job on the self-hosted runner was observed to leave an orphan <code>julia</code> process and a pathologically slow next run.</p></li><li><p>A cold job spends many minutes precompiling AD backends with no log output. Silence is not a hang.</p></li></ul><h3 id="The-probe-harness" tabindex="-1">The probe harness <a class="header-anchor" href="#The-probe-harness" aria-label="Permalink to &quot;The probe harness {#The-probe-harness}&quot;">​</a></h3><p><code>probe/gpu/probe_gpu.jl</code> (on <code>main</code>, run by the <code>GPU probe</code> workflow — <code>workflow_dispatch</code>, or a PR carrying the <code>run probe</code> label) is a standalone capability matrix: it exercises AD backends, RHS shapes and ensemble algorithms on real hardware and reports pass/expected-fail/fail per row. It exists because <strong>guessing what a device supports is unreliable</strong> — the step that mattered most in this project was measuring first and designing after. Two speculative blockers turned out not to exist, and the one real blocker was not anticipated at all.</p><p>Extend the probe before extending the implementation.</p><h3 id="The-fake-AbstractGPUArray-trap" tabindex="-1">⚠️ The fake <code>AbstractGPUArray</code> trap <a class="header-anchor" href="#The-fake-AbstractGPUArray-trap" aria-label="Permalink to &quot;⚠️ The fake `AbstractGPUArray` trap {#The-fake-AbstractGPUArray-trap}&quot;">​</a></h3><p>Device <em>dispatch</em> can be tested without a device by defining a CPU stand-in:</p><div class="language-julia vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">julia</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">struct</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> FakeGPUArray{T,N} </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">&lt;:</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> GPUArraysCore.AbstractGPUArray{T,N}</span></span>\n<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    data</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">::</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">Array{T,N}</span></span>\n<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">end</span></span></code></pre></div><p>with <code>size</code>/<code>getindex</code>/<code>setindex!</code>/<code>similar</code> — enough to exercise <code>_safe_only</code>, <code>_device_like</code>, <code>_buffer_like</code> and the split helpers on CPU.</p><p><strong>It stops being self-contained the moment GPUArrays.jl is loaded.</strong> The test session does <code>using CUDA</code> for every file, and GPUArrays overrides <code>copyto!</code>, <code>view</code> and <code>getindex</code> for <code>AbstractGPUArray</code> with device-kernel implementations the CPU stand-in cannot satisfy — a <code>StackOverflowError</code> in <code>copyto!</code>, &quot;not implemented&quot; in <code>view</code>/<code>getindex</code>. The fake must therefore also delegate those methods to its backing <code>Array</code>, <strong>and</strong> define <code>copyto!(::FakeGPUArray{T,N}, ::Array{T,N})</code> to resolve the ambiguity with GPUArrays&#39; <code>copyto!(::AnyGPUArray, ::Array)</code>.</p><p>Both fake-array test files already carry the fix; copy the pattern rather than rediscovering it.</p><hr><h2 id="Out-of-scope" tabindex="-1">Out of scope <a class="header-anchor" href="#Out-of-scope" aria-label="Permalink to &quot;Out of scope {#Out-of-scope}&quot;">​</a></h2><p>Recorded, not planned:</p><ul><li><p>a user-facing batched/ensemble <code>Flow</code> API (this is where the real speedup is);</p></li><li><p>AMDGPU and Metal — the coercion layer is written backend-agnostically against <code>AbstractGPUArray</code>, but nothing has been tested;</p></li><li><p>a GPU-specific default <code>alg</code> for the integrator;</p></li><li><p>an out-of-place augmented problem construction;</p></li><li><p>wiring <code>__consistent_initial_condition</code> into CTFlows for better device-mismatch errors.</p></li></ul><hr><h2 id="See-also" tabindex="-1">See also <a class="header-anchor" href="#See-also" aria-label="Permalink to &quot;See also {#See-also}&quot;">​</a></h2><ul><li><p><a href="./../flows/gpu">GPU flows</a> — the user guide.</p></li><li><p><a href="/CTFlows.jl/stable/api/internals#CTFlows.Systems._safe_only"><code>CTFlows.Systems._safe_only</code></a>, <a href="/CTFlows.jl/stable/api/internals#CTFlows.Systems._device_like"><code>CTFlows.Systems._device_like</code></a>, <a href="/CTFlows.jl/stable/api/internals#CTFlows.Systems._buffer_like"><code>CTFlows.Systems._buffer_like</code></a> — the coercion helpers.</p></li><li><p><a href="/CTFlows.jl/stable/api/flows#CTFlows.Flows.flow_registry"><code>CTFlows.Flows.flow_registry</code></a>, <a href="/CTFlows.jl/stable/api/internals#CTFlows.Flows._flow_description"><code>CTFlows.Flows._flow_description</code></a>, <a href="/CTFlows.jl/stable/api/internals#CTFlows.Flows._pop_method"><code>CTFlows.Flows._pop_method</code></a> — the routing surface.</p></li><li><p><a href="https://control-toolbox.org/CTBase.jl/stable/api/strategies#CTBase.Strategies" target="_blank" rel="noreferrer"><code>CTBase.Strategies</code></a>, <a href="https://control-toolbox.org/CTSolvers.jl/stable/api/integrators#CTSolvers.Integrators" target="_blank" rel="noreferrer"><code>CTSolvers.Integrators</code></a> — the parameterised strategy families.</p></li></ul>',72)])])}const g=t(i,[["render",n]]);export{u as __pageData,g as default};
